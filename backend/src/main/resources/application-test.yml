spring:
  application:
    name: bocrm-test
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
  ai:
    openai:
      api-key: test-key
    anthropic:
      api-key: test-key
    google:
      gemini:
        api-key: test-key

app:
  encryption:
    secret: "ldzfURh8vOcXmrDBnE/uZ20Av2uS6YiZxKaQO26dmAs="  # Test key for unit tests # gitleaks:allow
  opa:
    enabled: false

logging:
  level:
    root: WARN
    com.bocrm: DEBUG
