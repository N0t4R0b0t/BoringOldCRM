# Cache Configuration for BOCRM
# Dev Profile: Uses in-memory cache
cache.type=local

# Cache Settings
# For local/dev use the simple (in-memory) cache to avoid connecting to Redis
spring.cache.type=simple
# If you want Redis in prod, set spring.cache.type=redis in application-prod.properties
# spring.cache.redis.time-to-live=300000
